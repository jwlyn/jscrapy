!!org.jscrapy.core.config.JscrapyConfig
taskBaseConfig:
  taskId: test-id
  taskName: test-task_name
  groupId: TASK.VIRTUAL.ID

taskComponentConfigs:
  DEDUP_REDIS: !!org.jscrapy.ext.modulecfg.RedisDedupConfig
    host: ${redis.dedup.host}
    port: ${redis.port}
