taskBaseConfig:
  taskId: task.id
  taskName: task.name
  virtualId: TASK.VIRTUAL.ID
  dedup.mongo.host: ${mongo.dedup.host}
  dedup.mongo.port: ${mongo.dedup.port}
  rt.ext.mongodb.dedup.db.name: myspider_dedup
