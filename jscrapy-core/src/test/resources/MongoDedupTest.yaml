!!org.jscrapy.core.config.JscrapyConfig
taskBaseConfig:
  taskId: task.id
  taskName: task.name
  groupId: TASK.VIRTUAL.ID

taskComponentConfigs:
  DEDUP_MONGO: !!org.jscrapy.core.config.modulecfg.MongoDedepConfig
    dbName: jscrapy_test_dedup
    host: ${mongo.dedup.host}
    port: ${mongo.dedup.port}

