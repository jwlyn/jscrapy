!!org.jscrapy.core.config.JscrapyConfig
taskBaseConfig:
  taskId: task.id
  taskName: task.name
  virtualId: TASK.VIRTUAL.ID

taskComponentConfigs:
  DEDUP_MONGO: !!org.jscrapy.core.config.modulecfg.MongoDedepConfig
    dbName: jscrapy_mongo_cacher
    host: ${mongo.cacher.host}
    port: ${mongo.cacher.port}
