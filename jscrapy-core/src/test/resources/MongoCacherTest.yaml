taskBaseConfig:
  taskId: task.id
  taskName: task.name
  virtualId: TASK.VIRTUAL.ID
  cacher.mongo.host: ${mongo.cacher.host}
  cacher.mongo.port: ${mongo.cacher.port}
  rt.ext.mongodb.cacher.db.name: myspider_cacher

